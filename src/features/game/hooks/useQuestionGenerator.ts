// ---------------------------------------------------------------------------
// Stub — full implementation in Phase 2
// ---------------------------------------------------------------------------

/**
 * Convenience hook (if needed) for generating questions in a component
 * without directly importing the pure function.
 *
 * For Phase 1 this is an empty stub.
 */
export function useQuestionGenerator(): void {
  // Phase 2 implementation.
}
